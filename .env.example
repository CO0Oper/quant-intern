# Optional: only needed for direct Node API mode.
# CLI-agent mode can run through Claude Code or Codex without these repo-level keys.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=

# Optional: TradingView Desktop debug endpoint for health checks.
TV_CDP_URL=http://127.0.0.1:9222
